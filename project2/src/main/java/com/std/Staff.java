package com.std;

public class Staff extends FullTime {
    /**
     * Constructs <code>Staff</code> object with <code>role</code> and <code>department</code>.
     * @param role a role of user
     * @param department a department of the user
     */
    public Staff(String role, String department) {
        super(role, department);
    }
}